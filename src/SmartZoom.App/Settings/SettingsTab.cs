namespace SmartZoom.App.Settings;

/// <summary>Which tab the settings window opens on. Order matches the tabs as <see cref="SettingsForm"/> adds them.</summary>
internal enum SettingsTab
{
    Triggers,
    Applications,
    Zoom,
    Diagnostics,
}
