<!-- Placeholder. The triggers editor is ported onto this shell in the second wave; see Settings/TriggersPage.cs. -->
<UserControl x:Class="SmartZoom.App.Ui.Pages.TriggersPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:ClassModifier="internal">
    <StackPanel Margin="40,36,40,36">
        <TextBlock Style="{StaticResource Text.Hero}" FontSize="30" Text="Triggers" />
        <TextBlock Style="{StaticResource Text.Muted}"
                   Margin="0,10,0,0"
                   MaxWidth="560"
                   HorizontalAlignment="Left"
                   Text="The gestures that start a zoom. This page still lives in the old settings window; it moves here next." />
    </StackPanel>
</UserControl>
