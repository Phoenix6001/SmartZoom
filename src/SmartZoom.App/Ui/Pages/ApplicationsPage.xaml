<!-- Placeholder: the applications table is ported onto this shell in the second wave; see Settings/ApplicationsPage.cs. -->
<UserControl x:Class="SmartZoom.App.Ui.Pages.ApplicationsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:ClassModifier="internal">
    <StackPanel Margin="40,36,40,36">
        <TextBlock Style="{StaticResource Text.Hero}" FontSize="30" Text="Applications" />
        <TextBlock Style="{StaticResource Text.Muted}"
                   Margin="0,10,0,0"
                   MaxWidth="560"
                   HorizontalAlignment="Left"
                   Text="Which application is zoomed by which strategy. This page still lives in the old settings window; it moves here next." />
    </StackPanel>
</UserControl>
